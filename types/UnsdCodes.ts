export enum UnGroupings {
  LDC = "LDC",
  LLDC = "LLDC",
  SIDS = "SIDS",
}

export enum UnLevel {
  Countries = "countries",
  Regions = "regions",
  SubRegions = "subRegions",
  IntermediateRegions = "intermediateRegions",
}
