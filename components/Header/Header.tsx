/** @jsxImportSource theme-ui */

import { FC } from "react";
import Navigation from "../Navigation";

const Header: FC = () => {
  return (
    <header>
      <Navigation />
    </header>
  );
};

export default Header;
