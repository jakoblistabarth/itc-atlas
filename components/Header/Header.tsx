import { FC } from "react";
import Navigation from "../Navigation";

const Header: FC = () => {
  return (
    <header className="mt-5">
      <Navigation />
    </header>
  );
};

export default Header;
